$("#<%= @image_id %>").after("<%=j render "posts/carousel_image", image: @image.image, id: @image_id %>");

$("#carousel_images").animate(
  { width: 500 },
  { duration: 1000 }
);

$("#<%= @image_to_remove %>").remove();
